# SMTP configuration for sending emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username@example.com
SMTP_PASS=yourpassword

# Email address that will receive form submissions
SERVICE_EMAIL=service@it-express.org
